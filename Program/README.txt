A small quick and dirty program, which runs through each file inside a defined folder and looks for native functions and events, 
then converts these functions/evnets to sublime-snippets.

It has problems with ObjectReference.psc line 89 and line 100.
Deleting the comments on these lines fixes that.